<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login | CSE Motors</title>
  <link rel="stylesheet" href="/css/styles.css">
  <style>
    body { background: #f8f9fa !important; background-image: none !important; }
    .container {
      max-width: 420px; margin: 4rem auto; padding: 3rem;
      background: white; border-radius: 16px; box-shadow: 0 15px 40px rgba(0,0,0,0.15);
      text-align: center;
    }
    h1 { margin-bottom: 2rem; }
    input { width: 100%; padding: 1rem; margin: 0.8rem 0; border: 2px solid #ddd; border-radius: 8px; }
    button {
      background: #1976d2; color: white; padding: 1rem 2.5rem; font-size: 1.3rem; border: none; border-radius: 50px;
      cursor: pointer; margin-top: 1rem; font-weight: bold;
    }
    .register-link {
      margin-top: 2rem; padding: 1.2rem; background: #e3f2fd; border: 3px solid #1976d2;
      border-radius: 12px; font-size: 1.3rem;
    }
    .register-link a { color: #1976d2; font-weight: bold; text-decoration: underline; }
    .error { color: #d32f2f; background: #ffebee; padding: 1rem; border-radius: 8px; margin: 1rem 0; }
  </style>
</head>
<body>
  <%- include('../partials/header') %>
  <%- include('../partials/navigation') %>

  <main>
    <div class="container">
      <h1>Login to CSE Motors</h1>

      <% if (locals.error) { %>
        <div class="error"><%= locals.error %></div>
      <% } %>

      <form action="/account/login" method="post">
        <p>
          <input type="email" name="account_email" required placeholder="Email" 
                 value="<%= locals.account_email || '' %>">
        </p>
        <p>
          <input type="password" name="account_password" required placeholder="Password">
        </p>
        <p>
          <button type="submit">Login</button>
        </p>
      </form>

      <div class="register-link">
        Don't have an account? <a href="/account/register">Register here</a>
      </div>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>

